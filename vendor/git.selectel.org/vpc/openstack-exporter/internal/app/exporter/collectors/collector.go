package collectors

const (
	// Top-level label for all collected metrics.
	namespace = "openstack"

	// Common labels that can be used for most metrics.
	idLabel        = "id"
	regionLabel    = "region"
	zoneLabel      = "zone"
	projectIDLabel = "project_id"
	accountLabel   = "account"
)
